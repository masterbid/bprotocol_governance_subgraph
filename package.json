{
  "name": "bprotocol-governance",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ bprotocol-governance",
    "create-local": "graph create --node http://localhost:8020/ bprotocol-governance",
    "remove-local": "graph remove --node http://localhost:8020/ bprotocol-governance",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 bprotocol-governance"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.21.0",
    "@graphprotocol/graph-ts": "0.20.0"
  }
}
